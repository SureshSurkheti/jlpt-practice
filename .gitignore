__pycache__/
*.pyc

# Downloaded dictionary sources, rebuilt by tools/build_kanji.py
.cache/

# Scratch page used when rasterising icons; must never ship.
_raster.html

# macOS Finder droppings. One was tracked, so it changed every time a folder
# was opened.
.DS_Store

# docs/ is tracked. It holds study material this project wrote and generated -
# the grammar and listening phrase books, and the vocabulary notes. Nothing
# from anyone else goes in there: no scanned papers, no publisher's material.
